apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx_test.fullname" . }}
  labels:
    {{- include "nginx_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx_test.selectorLabels" . | nindent 4 }}
